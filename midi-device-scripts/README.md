# MIDI Device Scripts (Lua)

Scripter is JavaScript. MIDI Device Scripts are Lua. Do not swap them.

Apple documents that MDS exist and auto-assign USB controllers. Apple does
not publish the Lua host API — that API is UNKNOWN. E07 is live-unknown.

Do not drop a reverse-engineered `config.lua` here until E07 has a probe.
